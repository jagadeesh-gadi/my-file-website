// normal object type macros in that normal
// object like macro(no aruguments)
// use constants,register address,configuration address

#include <stdio.h>
#define NUM_SIZE 5

int main()
{

    char a[NUM_SIZE];
    printf("%s", a);

    return 0;
}